1、公告模块
   添加以及修改公告实现文件异步上传(只上传图片)，需要通过插件 jquery.form.js


2、文档模块
   a、根据文档标题实现文档分页查询
   b、删除文档，删除文档时需要将文件同时也删除
   c、添加文档，引入方表单重复提交拦截器，文件上传拦截器，限制文件上传的类型，文件信息不合法
      需要给出中文提示信息
   d、修改文档，引入方表单重复提交拦截器，文件上传拦截器，限制文件上传的类型，文件信息不合法
      需要给出中文提示信息，如果在修改文档的时候选择了新的文档，需要将历史文件删除

   e、下载文档，防止中文乱码
       	

	// 对浏览器进行判断(msie)
	String userAgent =  ServletActionContext.getRequest().getHeader("user-agent");
	String title = document.getTitle();
	if (userAgent.toLowerCase().indexOf("msie") != -1){
		title = URLEncoder.encode(title, "utf-8"); // msie
	}else{
		title = new String(title.getBytes("utf-8"), "iso8859-1");
	}

3、员工模块
   实现员工多条件分页查询